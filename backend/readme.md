add maven dependency to intellij
File > Project Structure > Libraries > '+' button > com.google.code.gson 12.2.1
if Gson or other packages not found, 
1. Ctrl+Shift+A to find actions (Cmd+Shift+A for Mac)
2. type "reload"
3. Select "Reload All Maven Projects"